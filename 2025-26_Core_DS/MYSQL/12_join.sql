use startersql;

-- SELECT users.name, adderss.city
-- FROM users
-- INNER JOIN adderss ON users.id = adderss.user_id;

-- select users.name, users.email, adderss.city
-- from users left join adderss on users.id=adderss.user_id;

-- select users.name, users.salary, adderss.city
-- from users right join adderss on users.id=adderss.user_id;	

select * from users;

SELECT * FROM adderss;