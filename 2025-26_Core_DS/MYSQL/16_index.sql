use startersql;

-- show indexes from users;

-- select * from users where email='alice@example.com' and gender='Female';

-- create index idx_gender on users(gender);

-- drop index idx_gender on users;